# Dev API calls use the Vite dev server (port 5173); /api is proxied to :1337.
# Do not set VITE_API_URL here — it overrides .env.local. Use .env.local or .env.development.local instead.
# VITE_CLIENT_URL=http://localhost:5173
